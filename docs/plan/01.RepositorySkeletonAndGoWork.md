# Task 1 — Repository Skeleton and go.work

## Context

This is the first task in the CloudForge Private Network implementation plan. The goal is to create the complete repository skeleton: all Go module `go.mod` files, the `go.work` workspace file, and the empty directory structure that every subsequent task will populate.

The architecture is defined in `docs/cf-private-network.md`, specifically section 14 (Golang Project Structure with go.work). The module root is `github.com/jtomasevic/cloud-forge-2`.

## Dependencies

None. This is the foundation task.

## Prompt

You are implementing the repository skeleton for **CloudForge**, an open-source multi-tenant cloud platform. Your task is to create all Go module files, the `go.work` workspace file, and the empty directory structure as defined below. Do not implement any Go code yet — only create the module scaffolding, directory structure, and placeholder files where required.

### Repository root: `/Users/jtomasevic/my-repos/cloud-forge-2`

### Go version: 1.26

### Module root: `github.com/jtomasevic/cloud-forge-2`

---

### Directory and module structure to create

Create the following `go.mod` files, each with the correct module path and `go 1.26` directive. Add no dependencies yet — dependencies will be added by subsequent tasks as needed.

**Services:**
- `services/cf-router/go.mod` → module `github.com/jtomasevic/cloud-forge-2/services/cf-router`
- `services/cf-accounts/go.mod` → module `github.com/jtomasevic/cloud-forge-2/services/cf-accounts`
- `services/cf-provisioner/go.mod` → module `github.com/jtomasevic/cloud-forge-2/services/cf-provisioner`

**Libraries:**
- `libs/cloudforge-core/go.mod` → module `github.com/jtomasevic/cloud-forge-2/libs/cloudforge-core`
- `libs/scylladb/go.mod` → module `github.com/jtomasevic/cloud-forge-2/libs/scylladb`
- `libs/openbao/go.mod` → module `github.com/jtomasevic/cloud-forge-2/libs/openbao`
- `libs/clients/cf-accounts/go.mod` → module `github.com/jtomasevic/cloud-forge-2/libs/clients/cf-accounts`
- `libs/clients/cf-provisioner/go.mod` → module `github.com/jtomasevic/cloud-forge-2/libs/clients/cf-provisioner`

**Tools:**
- `tools/cf-cli/go.mod` → module `github.com/jtomasevic/cloud-forge-2/tools/cf-cli`
- `tools/migrations/go.mod` → module `github.com/jtomasevic/cloud-forge-2/tools/migrations`

---

### go.work file

Create `go.work` at the repository root:

```
go 1.26

use (
    ./services/cf-router
    ./services/cf-accounts
    ./services/cf-provisioner
    ./libs/cloudforge-core
    ./libs/scylladb
    ./libs/openbao
    ./libs/clients/cf-accounts
    ./libs/clients/cf-provisioner
    ./tools/cf-cli
    ./tools/migrations
)
```

---

### Directory structure to create

For each module, create the directory structure below. Create empty `.gitkeep` files only in leaf directories that need to exist but contain no Go files yet. Do NOT create any `.go` files in this task — those come in later tasks.

```
api/
  cf-accounts/v1/
  cf-provisioner/v1/
  cf-router/v1/

services/
  cf-router/
    internal/
      rest/generated/
      service/
      repository/apikeys/
  cf-accounts/
    internal/
      rest/generated/
      service/
      repository/accounts/
      repository/tenants/
      repository/networks/
      repository/credentials/
  cf-provisioner/
    internal/
      rest/generated/
      service/
      repository/vcluster/
      repository/cidr/
      repository/cilium/
      repository/gateway/
      repository/kubeconfig/
      repository/jobs/

libs/
  cloudforge-core/pkg/tenant/
  cloudforge-core/pkg/network/
  cloudforge-core/pkg/errors/
  cloudforge-core/pkg/middleware/
  scylladb/pkg/client/
  scylladb/pkg/migrate/
  openbao/pkg/client/
  clients/cf-accounts/v1/
  clients/cf-provisioner/v1/

tools/
  cf-cli/
  migrations/scripts/

dev/
```

---

### .gitignore additions

Ensure the following entries exist in the root `.gitignore` (add them if missing, do not remove existing entries):

```
# Go workspace
go.work.sum

# Build output
bin/

# Generated files are committed, but local tool binaries are not
tools/bin/
```

---

### Makefile

Create a `Makefile` at the repository root with the following initial targets. Use tabs for indentation (required by Make).

```makefile
.PHONY: all build test lint codegen

all: build

build:
	go build ./...

test:
	go test -race -count=1 ./...

lint:
	go vet ./...

codegen:
	go generate ./...
```

---

### README.md at repository root

Create `README.md` at the repository root with the following content:

```markdown
# CloudForge

An open-source multi-tenant cloud platform built on Kubernetes.

## Repository structure

- `api/` — OpenAPI 3.0.3 specifications (source of truth for HTTP contracts)
- `services/` — CloudForge platform services (cf-router, cf-accounts, cf-provisioner)
- `libs/` — Shared libraries and generated client SDKs
- `tools/` — CLI tools and schema migration runner
- `dev/` — Local development environment configuration

## Prerequisites

go 1.26+, Docker, k3d, kubectl, Helm, Tilt, oapi-codegen v2.

See `docs/plan/README.md` for the full development setup guide.

## Quick start

See `docs/plan/README.md`.
```

---

## Acceptance Criteria

- `go work sync` runs without error from the repository root
- `go build ./...` runs without error (no source files yet, so this should produce no output)
- All directories listed above exist
- No `.go` files exist yet (except empty `go.mod` files)
- The `go.work` file references all 10 modules
