# Task 27 — CF App Service Codegen And Client Updates

## Context

After the CF App Service OpenAPI contract is defined, generated server stubs and clients must be refreshed. Generated code must not be edited manually.

Reference:

- `docs/rest-services-rules.md`
- `api/cf-provisioner/v1/openapi.yaml`
- `services/cf-provisioner/generate.go`
- `libs/clients/cf-provisioner/v1/client.gen.go`

## Requirements

1. Run code generation after the OpenAPI changes from Task 26.
2. Ensure generated CF-Provisioner server interfaces include app-service handlers.
3. Ensure generated CF-Provisioner client supports app-service routes.
4. Update compile-time handler assertions if new methods are required.
5. Keep generated files untouched except through codegen.
6. Update any aggregated Swagger/OpenAPI handling if new paths must appear in public docs.

## Files Likely To Change

- `services/cf-provisioner/internal/rest/generated/server.gen.go`
- `libs/clients/cf-provisioner/v1/client.gen.go`
- `services/cf-provisioner/internal/rest/handler.go`
- `services/cf-provisioner/internal/rest/server.go`
- `services/cf-provisioner/internal/rest/README.md`
- `libs/clients/cf-provisioner/README.md` if present or newly needed

## Code Comments And READMEs

- Add or update package README files that explain what generated packages contain.
- Do not add comments inside generated files.
- Add comments only around hand-written handler stubs where the temporary behavior is not obvious.

## Acceptance Criteria

- `make codegen` produces deterministic output.
- `git diff` after a second `make codegen` is clean.
- CF-Provisioner compiles with the new generated interfaces.
- Generated client compiles.

## Validation

Run:

```bash
make codegen
git diff --check
make build
make test
```

